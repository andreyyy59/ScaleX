package me.proyecto.scalex.data.mapper

import me.proyecto.scalex.data.local.FavoriteEntity
import me.proyecto.scalex.data.local.MotorcycleEntity
import me.proyecto.scalex.data.remote.MotorcycleDto
import me.proyecto.scalex.domain.model.Motorcycle

fun MotorcycleDto.toDomain(): Motorcycle = Motorcycle(
    make = make,
    model = model,
    year = year,
    type = type,
    displacement = displacement,
    engine = engine,
    power = power,
    torque = torque,
    topSpeed = topSpeed,
    compression = compression,
    boreStroke = boreStroke,
    valvesPerCylinder = valvesPerCylinder,
    fuelSystem = fuelSystem,
    fuelControl = fuelControl,
    ignition = ignition,
    lubrication = lubrication,
    cooling = cooling,
    gearbox = gearbox,
    transmission = transmission,
    clutch = clutch,
    frame = frame,
    frontSuspension = frontSuspension,
    frontWheelTravel = frontWheelTravel,
    rearSuspension = rearSuspension,
    rearWheelTravel = rearWheelTravel,
    frontTire = frontTire,
    rearTire = rearTire,
    frontBrakes = frontBrakes,
    rearBrakes = rearBrakes,
    totalWeight = totalWeight,
    totalHeight = totalHeight,
    totalLength = totalLength,
    totalWidth = totalWidth,
    seatHeight = seatHeight,
    wheelbase = wheelbase,
    groundClearance = groundClearance,
    fuelCapacity = fuelCapacity,
    starter = starter,
    fuelConsumption = fuelConsumption,
    emission = emission,
    dryWeight = dryWeight
)

fun Motorcycle.toEntity(): MotorcycleEntity = MotorcycleEntity(
    id = getId(),
    make = make,
    model = model,
    year = year,
    type = type,
    displacement = displacement,
    engine = engine,
    power = power,
    torque = torque,
    topSpeed = topSpeed,
    compression = compression,
    boreStroke = boreStroke,
    valvesPerCylinder = valvesPerCylinder,
    fuelSystem = fuelSystem,
    fuelControl = fuelControl,
    ignition = ignition,
    lubrication = lubrication,
    cooling = cooling,
    gearbox = gearbox,
    transmission = transmission,
    clutch = clutch,
    frame = frame,
    frontSuspension = frontSuspension,
    frontWheelTravel = frontWheelTravel,
    rearSuspension = rearSuspension,
    rearWheelTravel = rearWheelTravel,
    frontTire = frontTire,
    rearTire = rearTire,
    frontBrakes = frontBrakes,
    rearBrakes = rearBrakes,
    totalWeight = totalWeight,
    totalHeight = totalHeight,
    totalLength = totalLength,
    totalWidth = totalWidth,
    seatHeight = seatHeight,
    wheelbase = wheelbase,
    groundClearance = groundClearance,
    fuelCapacity = fuelCapacity,
    starter = starter,
    fuelConsumption = fuelConsumption,
    emission = emission,
    dryWeight = dryWeight,
    lastUpdated = System.currentTimeMillis()
)

fun MotorcycleEntity.toDomain(): Motorcycle = Motorcycle(
    make = make,
    model = model,
    year = year,
    type = type,
    displacement = displacement,
    engine = engine,
    power = power,
    torque = torque,
    topSpeed = topSpeed,
    compression = compression,
    boreStroke = boreStroke,
    valvesPerCylinder = valvesPerCylinder,
    fuelSystem = fuelSystem,
    fuelControl = fuelControl,
    ignition = ignition,
    lubrication = lubrication,
    cooling = cooling,
    gearbox = gearbox,
    transmission = transmission,
    clutch = clutch,
    frame = frame,
    frontSuspension = frontSuspension,
    frontWheelTravel = frontWheelTravel,
    rearSuspension = rearSuspension,
    rearWheelTravel = rearWheelTravel,
    frontTire = frontTire,
    rearTire = rearTire,
    frontBrakes = frontBrakes,
    rearBrakes = rearBrakes,
    totalWeight = totalWeight,
    totalHeight = totalHeight,
    totalLength = totalLength,
    totalWidth = totalWidth,
    seatHeight = seatHeight,
    wheelbase = wheelbase,
    groundClearance = groundClearance,
    fuelCapacity = fuelCapacity,
    starter = starter,
    fuelConsumption = fuelConsumption,
    emission = emission,
    dryWeight = dryWeight
)

fun Motorcycle.toFavoriteEntity(userId: String): FavoriteEntity = FavoriteEntity(
    motorcycleId = getId(),
    userId = userId,
    make = make,
    model = model,
    year = year,
    type = type,
    displacement = displacement,
    engine = engine,
    power = power,
    torque = torque,
    topSpeed = topSpeed,
    compression = compression,
    boreStroke = boreStroke,
    valvesPerCylinder = valvesPerCylinder,
    fuelSystem = fuelSystem,
    fuelControl = fuelControl,
    ignition = ignition,
    lubrication = lubrication,
    cooling = cooling,
    gearbox = gearbox,
    transmission = transmission,
    clutch = clutch,
    frame = frame,
    frontSuspension = frontSuspension,
    frontWheelTravel = frontWheelTravel,
    rearSuspension = rearSuspension,
    rearWheelTravel = rearWheelTravel,
    frontTire = frontTire,
    rearTire = rearTire,
    frontBrakes = frontBrakes,
    rearBrakes = rearBrakes,
    totalWeight = totalWeight,
    totalHeight = totalHeight,
    totalLength = totalLength,
    totalWidth = totalWidth,
    seatHeight = seatHeight,
    wheelbase = wheelbase,
    groundClearance = groundClearance,
    fuelCapacity = fuelCapacity,
    starter = starter,
    fuelConsumption = fuelConsumption,
    emission = emission,
    dryWeight = dryWeight
)

fun FavoriteEntity.toDomain(): Motorcycle = Motorcycle(
    make = make,
    model = model,
    year = year,
    type = type,
    displacement = displacement,
    engine = engine,
    power = power,
    torque = torque,
    topSpeed = topSpeed,
    compression = compression,
    boreStroke = boreStroke,
    valvesPerCylinder = valvesPerCylinder,
    fuelSystem = fuelSystem,
    fuelControl = fuelControl,
    ignition = ignition,
    lubrication = lubrication,
    cooling = cooling,
    gearbox = gearbox,
    transmission = transmission,
    clutch = clutch,
    frame = frame,
    frontSuspension = frontSuspension,
    frontWheelTravel = frontWheelTravel,
    rearSuspension = rearSuspension,
    rearWheelTravel = rearWheelTravel,
    frontTire = frontTire,
    rearTire = rearTire,
    frontBrakes = frontBrakes,
    rearBrakes = rearBrakes,
    totalWeight = totalWeight,
    totalHeight = totalHeight,
    totalLength = totalLength,
    totalWidth = totalWidth,
    seatHeight = seatHeight,
    wheelbase = wheelbase,
    groundClearance = groundClearance,
    fuelCapacity = fuelCapacity,
    starter = starter,
    fuelConsumption = fuelConsumption,
    emission = emission,
    dryWeight = dryWeight
)
